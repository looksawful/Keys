{
  "name": "keys-browser",
  "version": "1.0.0",
  "private": true,
  "description": "Browser hotkey trainer.",
  "main": "scripts/browser-server.cjs",
  "scripts": {
    "start": "npm run browser:start",
    "test": "node --test",
    "check:syntax": "node --check src/renderer/app.js && node --check src/renderer/data.js && node --check src/renderer/logic.js && node --check scripts/browser-build.cjs && node --check scripts/browser-server.cjs",
    "browser:build": "node scripts/browser-build.cjs",
    "browser:start": "node scripts/browser-server.cjs",
    "check": "npm run check:syntax && npm test && npm run browser:build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
